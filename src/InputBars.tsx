import React from 'react';
/**
 * Will be taking in type, placeholder, value, function
 * 
 * InputBar is where the person inputs their solutions
 * - Remember I have to hold that value and put it into the database
 */

const InputBar: React.FC = () => {
    return <div></div>
}

export default InputBar